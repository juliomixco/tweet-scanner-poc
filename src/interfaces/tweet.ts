export interface ITweetAPIConfig {
  consumerKey: string;
  consumerSecret: string;
  // accessTokenKey and  accessTokenSecret are optional but you will get limited access
  accessTokenKey?: string;
  accessTokenSecret?: string;
}
