{
  "name": "tweet-scanner",
  "version": "1.0.0",
  "description": "scan tweets by url",
  "main": "scanner.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node ./build/main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.0.6",
    "dotenv": "^8.2.0",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "typescript": "^3.9.3"
  }
}
